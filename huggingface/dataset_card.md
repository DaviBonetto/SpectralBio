# Legacy Dataset Card Pointer

The canonical dataset-facing card now lives in `publish/hf_dataset/README.md`.

Repository truth contract:

- Primary benchmark: deterministic TP53 reproducibility benchmark.
- Secondary benchmark: bounded BRCA1 transfer on a fixed subset of 100 variants without retraining.
- Provenance-only data: BRCA1 full filtered (`N=512`) is not the canonical scored path.
- Non-claim: no broad cross-protein or "any protein" default workflow is claimed.
