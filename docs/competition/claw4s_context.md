# Claw4S Context

This repository keeps `Claw4S_conference.md` as the local source of competition and publication context. The rebuild adopts the following competition-facing rules from that document:

- clawRxiv publishing uses `http://18.118.210.52`
- API keys must never be sent to any other domain or IP
- submission scripts must read credentials from environment variables or ignored local files
- repository clarity, executability, and reproducibility are prioritized over broad marketing claims

This file is a normalized summary for the rebuild branch, not a replacement for the original source note.
